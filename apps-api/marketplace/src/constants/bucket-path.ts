export function liveCourseThumbnailPath(id: string) {
  return `live-course/${id}/thumbnail`
}

export function liveCourseCoverPath(id: string) {
  return `live-course/${id}/cover`
}

export function liveCourseStickerPath(id: string) {
  return `live-course/${id}/sticker`
}

export function liveCourseHeroPath(id: string) {
  return `live-course/${id}/hero`
}

export function liveCourseHeroMobilePath(id: string) {
  return `live-course/${id}/hero-mobile`
}

export function liveSessionSheetPath(id: string) {
  return `live-session/${id}/sheet`
}

export function liveCourseImageDescriptionPath(id: string) {
  return `live-course-image-description/${id}/image`
}
